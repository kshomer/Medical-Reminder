import { Context } from 'telegraf';

export async function help(ctx: Context) {
  await ctx.reply(
    `💊 <b>Med Reminder Bot - Помощник для приёма лекарств</b>\n\n` +
      `📋 <b>Команды:</b>\n\n` +
      `/start - Начать работу с ботом\n` +
      `/add - Добавить новое лекарство\n` +
      `/list - Список всех ваших лекарств\n` +
      `/stats - Статистика приёма лекарств\n` +
      `/delete - Удалить лекарство\n` +
      `/help - Показать это сообщение\n\n` +
      `🔔 <b>Уведомления:</b>\n` +
      `Бот автоматически присылает напоминания о приёме лекарств в указанное время.\n\n` +
      `После получения напоминания:\n` +
      `• Нажмите "✅ Принял" после приёма\n` +
      `• Нажмите "⏰ Напомнить через 15 минут" для отложения\n` +
      `• Нажмите "❌ Пропустить" если не приняли\n\n` +
      `📊 Вся статистика сохраняется и доступна через /stats`,
    { parse_mode: 'HTML' }
  );
}